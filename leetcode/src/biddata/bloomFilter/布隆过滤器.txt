使用多个hash方法，分别对每个存储的值计算hash值，在每个位置上标记存在

判断某值不存在：
    分别使用这些hash算法，计算出对应位置，如果有一个位置未被标记，则不存在；
    如果每个位置都被标记，可以说该值存在，但是存在失误率

计算布隆过滤器各个属性：
    预计元素个数：n
    允许失误率：p
    大小：-n*lnp/(ln2)^2
    hash函数个数：0.7 * m/n
    真实的失误率：（1-e^(nk/m)）^k
